---
interface Props {
  id: string;
}

const { id } = Astro.props;
---

<span class="sidenote-wrapper">
  <sup id={`fnref-${id}`} class="sidenote-ref">
    <a href={`#fn-${id}`} class="margin-number"></a>
  </sup>
  <span id={`fn-${id}`} class="margin-content">
    <a href={`#fnref-${id}`} class="footnote-backref" aria-label="Back to reference">↩</a>
    <slot />
  </span>
</span>
